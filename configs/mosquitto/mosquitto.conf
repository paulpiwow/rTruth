# Dev broker config — anonymous access is fine for local work.
# Add a password_file / ACL when this leaves a laptop.
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type all
